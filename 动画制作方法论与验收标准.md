# 动画制作方法论与验收标准

这份文件是树结构课程动画的制作协议。它解决的不是某一个旋转动画的 bug，而是规定：一个动画应该怎样被拆成镜头，状态应该怎样产生，运动应该怎样从状态推导，以及完成之后凭什么判定合格。

## 一、核心定义

动画不是“画出第一张图，再画出最后一张图，然后把两张图拼起来”。

一个镜头是一个有明确时间范围的连续状态函数：

```text
画面 = Render(State(t), Layout(t), Style(t), Camera(t))
```

其中 `t` 是镜头内的时间。镜头从第一帧到最后一帧，每一帧都必须能够解释为上一帧的连续演变。第一状态和最后状态只是运动的边界条件，不是可以直接拼接的两张独立图片。

本项目采用以下判断：

- 能够从一个状态逐步解释到另一个状态的内容，属于同一个镜头。
- 两个状态之间没有合理的连续过程，或者需要重新介绍例子、切换观察角度、清空画面，必须拆成两个镜头。
- 硬切只允许发生在镜头边界。镜头内部不能用整张画面突然替换、跨帧跳跃或交叉淡化来掩盖没有设计过渡的问题。
- 镜头内部可以有停顿、摘下、旋转、重新挂载、淡入淡出，但每个阶段的结束状态必须等于下一阶段的开始状态。

因此，“有关键帧”不等于“有动画”。关键帧只能定义语义状态和阶段边界；中间状态必须由规则计算出来，并逐帧采样。

## 二、适用的技术栈

继续使用当前工程已经验证过的、可复现的生产链，不为了动画效果引入无法检查的黑盒框架：

- **状态与动画规划：** Python 3，`dataclasses`、类型标注和标准库 `math`。状态、布局和运动计划尽量写成确定性的纯函数。
- **矢量素材：** SVG。使用 `rsvg-convert` 栅格化，适合节点、边、标记和结构图。
- **逐帧合成：** Pillow。主视频的每一帧由统一的 `render_frame(t)` 生成。
- **视频编码与合成：** FFmpeg。透明过程素材使用带 alpha 的 WebM；最终课程视频使用统一参数的 MP4。
- **媒体检查：** `ffprobe` 检查流参数，Pillow 检查真实栅格帧，FFmpeg 抽帧生成检查图和接缝附近的帧带。
- **生产入口：** `scripts/generate_tree_media.py` 生成独立素材，`scripts/build_avl_video.py` 负责分段渲染、拼接和配音封装。

推荐的模块边界如下：

```text
语义状态 Model
    ↓
布局求解 LayoutSolver
    ↓
运动规划 MotionPlan
    ↓
镜头时间线 ShotTimeline
    ↓
逐帧渲染 Renderer
    ↓
媒体编码与验收 MediaBuild / Verification
```

每个非平凡模块都应通过小而明确的接口连接。至少保留一个生产实现和一个测试/记录实现：

- 生产实现计算真实布局、运动和画面。
- 测试实现只记录输入状态、轨迹、边界和调用顺序，用来验证消费者，不依赖像素结果。

镜头通过注册表或命令行选择参与构建；不能把所有素材和所有镜头不可分割地写死在一个入口函数中。共享模块修改后，必须根据依赖关系重新生成所有引用它的素材。

## 三、先定义状态，再写运动

### 3.1 语义状态

先写清楚画面中的事实，不要先摆坐标：

- 树的根、节点关键字、父子关系和中序顺序。
- 每个节点的语义角色，例如天平上端、左货物、中货物、右货物、待插入节点、后继节点。
- 哪些节点存在，哪些节点正在离开，哪些节点尚未出现。
- 当前要解释的不变量，例如中序顺序不变、货物的左中右相对顺序不变、边只连接真实的父子关系。
- 口播正在回答的一个问题，以及这一镜头结束时应该得到的结论。

语义状态必须能独立于绘制代码被检查。绘制函数不能通过“当前时间落在哪个 `if` 分支”来临时猜测树的结构。

### 3.2 几何状态

对每一个语义状态调用布局求解器，得到完整的几何状态：

- 节点中心、文字包围盒和可见边界。
- 子树的真实包围盒和锚点。
- 边的端点，端点应由节点或子树的可见边界计算。
- 杠杆向量、旋转中心、挂载点和镜头范围。
- 相机平移、缩放和裁切区域。

几何状态必须从语义状态、视口和真实包围盒推导。不能在不同函数中分别维护一份“差不多的坐标表”。

### 3.3 渲染状态

颜色、描边、发光、透明度和字幕是表现层。它们不能偷偷改变语义几何，也不能把本来不存在的运动补出来。

例如一条边正在摘下，可以连续降低透明度；但边的两端仍然来自当前节点位置。一个节点被删除，可以连续淡出或沿已定义的路径离开；不能在某一帧从树中消失，然后让后续代码把其他节点突然挪到空位。

## 四、镜头设计协议

每个镜头在写代码前先填写以下镜头卡。镜头卡是制作输入，不是事后解释。

```text
镜头 ID：
为什么现在讲这个：它承接上一步的什么问题？
这一镜头只回答：
口播范围：
开始语义状态：
结束语义状态：
必须保持的不变量：
可见对象及其稳定 ID：
每个对象允许的运动：
阶段顺序：
阶段之间的连接状态：
镜头外才允许发生的变化：
布局求解方式：
运动参数的推导方式：
需要检查的帧：
```

### 4.1 一个镜头的阶段

阶段可以是以下几类：

- **保持：** 状态不变，只让观众读图或听口播。
- **连续移动：** 节点、文字、子树或相机从一个几何状态移动到另一个几何状态。
- **结构交接：** 先连续淡出旧边或旧挂点，再让对象沿连续路径进入新的挂点。
- **旋转：** 对定义好的局部对象应用由前后状态推导出的变换。
- **渐显/渐隐：** 对对象的可见性做连续变化，不能代替没有路径的整图切换。

每一阶段都要有 `start_state` 和 `end_state`。下一阶段的 `start_state` 必须直接复用上一阶段的 `end_state`，不能重新估一个坐标。

阶段内部使用半开时间区间，例如 `[t0, t1)`；镜头最后用同一个规范的结束状态收尾。这样不会因为两个 `if` 分支的边界条件不同而重复、漏帧或突然换图。

### 4.2 什么时候必须拆镜头

以下情况不要强行补一条虚假的运动：

- 例子本身改变，前一个例子的节点不再对应后一个例子的节点。
- 观察角度或布局尺度改变，且没有教学上需要展示的相机运动。
- 需要清空画面后重新建立结构。
- 两套图的对象 ID、拓扑和不变量无法建立对应关系。
- 连续变换会让观众误以为算法做了实际上没有做的操作。

此时建立明确的镜头边界，使用有意的硬切或单独制作一个“离场/入场”镜头。不要用交叉淡化把两个无关状态伪装成同一过程。

## 五、几何推导规则

### 5.1 禁止把结构运动写成固定角度

旋转多少度不是视觉常数，而是前后几何状态的结果。以两点组成的杠杆为例：

```python
v0 = (before_lower.x - before_upper.x,
      before_lower.y - before_upper.y)
v1 = (after_lower.x - after_upper.x,
      after_lower.y - after_upper.y)
angle = atan2(cross(v0, v1), dot(v0, v1))
```

再把角度归一化到最短有向旋转范围。旋转阶段对这个 `angle` 做时间插值，不能写死 `90`、`-90` 或任何“这类树通常是九十度”的假设。

如果前后杠杆中心不同、长度不同，应分别由前后端点推导中心平移、长度缩放和方向变化。一个可行的局部变换是：

```text
c(u) = lerp(c0, c1, easing(u))
r(u) = lerp(1, |v1| / |v0|, easing(u))
p(u) = c(u) + rotate(p0 - c0, angle * easing(u)) * r(u)
```

末帧直接满足后状态是数值闭合，不是“旋转过头后再用另一段动画拉回来”。如果必须做非刚体的结构重组，就把它拆成明确的连续阶段，并保证每个阶段的端点相接。

### 5.2 禁止把节点间距写成固定偏移

`节点间距 = 某个固定数字`、`货物下落 = 固定 180px`、`左右移动 = 固定 ±150px` 都不是布局方法。它们只在某一组尺寸、字体和视口下碰巧成立，换一个子树就可能重叠、越界或出现二次校正。

布局求解应至少考虑：

- 节点和文字的实际包围盒。
- 每棵子树的真实宽度、深度和可见边界。
- 父子边的最小可见长度。
- 同层对象的非重叠约束。
- 视口边界、字幕区域和经验栏等保留区域。

实际位置应由这些约束求解出来。可以定义“最小安全间隙”作为约束或样式参数，但不能把它直接当成所有节点的位移。节点尺寸属于视觉样式；结构距离必须由内容尺寸和布局约束决定。

### 5.3 一个属性只能有一个运动所有者

位置、角度、缩放、透明度和文字位置在一个时间段内都必须有唯一的写入者：

- `LayoutSolver` 负责目标几何。
- `MotionPlan` 负责从当前几何到目标几何的轨迹。
- `Renderer` 只读取采样结果并绘制。
- `Camera` 只负责相机变换，不能偷偷修正节点坐标。

禁止先由旋转函数修改位置，再由布局函数重新排版，再由渲染函数为了避免重叠追加一次 `lerp`。这种多处写入正是“旋转过头后又被调整回来”的根源。

同一个对象不能同时挂在两条互不知情的轨迹上。需要合成多个运动时，先在运动规划层生成一条明确的合成轨迹，再交给渲染器。

## 六、连续帧的实现要求

### 6.1 每帧从同一套计划采样

正确的结构是：

```python
before_layout = solve(before_state, viewport)
after_layout = solve(after_state, viewport)
plan = make_plan(before_state, after_state, before_layout, after_layout)

for frame_index in range(frame_count):
    u = frame_index / (frame_count - 1)
    render(plan.sample(u))
```

`sample(0)` 必须等于开始状态，`sample(1)` 必须等于结束状态。几何计算保留浮点数，最后栅格化时才取整；不能在不同阶段分别取整后再继续计算。

允许使用 easing 改变速度，但 easing 只能改变时间参数，不能改变目标状态。阶段连接处至少满足位置、尺寸、透明度等可见属性的连续性；如果没有明确的停顿，速度也应连续，避免阶段交界处突然加速。

### 6.2 结构变化也要有过渡

拓扑不能直接从旧图替换为新图。根据语义选择可见过渡：

- 新节点从队列、父节点或明确的起点连续移动到目标位置。
- 删除节点先沿合理路径离开或连续淡出，再改变其语义存在性。
- 边的增加、删除用连续透明度表达，并且边端点始终跟随节点。
- 后继替换要区分“关键字改变”和“节点框移动”；如果教学要求节点框不动，就只让关键字在两个已知位置之间连续交换。
- 子树摘下时保持子树内部相对结构；重新挂载时从当前挂载位置连续移动到目标挂载位置。

“上一帧画完整旧树，下一帧画完整新树”不属于结构过渡，即使两帧之间做了淡化也不属于结构过渡。

### 6.3 不能依赖稀疏检查点证明连续

只检查开头、中间和结尾三张图无法发现跳帧。渲染器必须实际生成完整帧序列；自检至少要逐帧检查运动轨迹和相邻帧变化，并在每个阶段边界附近加密检查。

把一个已有视频按口播时间任意拉伸或跳取帧，只有在以下条件同时满足时才允许：源视频的开始/结束状态与目标镜头一致，源视频各阶段边界已标注，映射不会跳过必须展示的中间状态。否则应回到源脚本重新生成，而不是在合成阶段补救。

## 七、生产工作流

### 第一步：从口播命题开始

每个段落先写“为什么现在讲这个”，明确它承接上一步的什么问题。每个结论都说明“它解决了上一步的什么问题”。动画只服务于当前命题，不因为有空白时间就加入无语义的运动。

### 第二步：画状态图而不是坐标图

列出开始状态、结束状态、对象 ID、结构差异和不变量。先确认算法语义正确，再确认画面能否连续连接。

### 第三步：划分镜头和阶段

标出可以连续连接的最大范围。能连接的内容合并为一个镜头；不能连接的内容明确切开。给每个阶段记录起止状态和口播时间，不用字幕时间表代替运动设计。

### 第四步：为前后状态分别求解布局

布局函数只看状态、视口和真实尺寸。不要先为动画猜一组坐标，再要求动画把这组坐标解释成结构。

### 第五步：生成运动计划

由前后布局计算角度、方向、长度、平移、缩放、挂载点和路径。对共享对象建立稳定 ID；对新增、删除和交接对象建立明确的出现、离开和连接过程。

### 第六步：逐帧渲染和自动检查

先输出独立 WebM 或逐帧 PNG，确认镜头本身合格，再接入主视频。共享动画函数修改后，列出全部调用方并按正确顺序重生成依赖资产。

### 第七步：接入口播并复查

确认镜头边界、阶段边界和口播命题一致。成片检查必须使用当前录音、当前 ASR 时间线和最新生成的视觉资产；不能只因为旧的成片文件存在就认为它已经包含最新修改。

## 八、自我检查方法

### 8.1 代码级检查

- 搜索结构运动代码，确认不存在把旋转操作写成固定角度的逻辑。
- 搜索节点、子树、货物之间的结构位移，确认不存在用固定偏移代替布局求解的逻辑。
- 确认一个属性在一个阶段只有一个运动所有者。
- 确认所有阶段边界显式记录，且后一阶段的输入复用前一阶段的输出。
- 确认 `sample(0)`、`sample(1)` 与规范状态逐字段相等，允许的仅是浮点误差。
- 确认渲染函数不修改状态、不重新决定拓扑、不追加隐藏的补偿运动。
- 确认每个生成资产都能从项目内脚本和明确命令重新生成。

### 8.2 状态和几何检查

对镜头内每一帧或每个采样点检查：

- 节点 ID 不会无故消失、重生或换身份。
- 树的父子关系、中序顺序和镜头声明的不变量成立。
- 旋转后的实际向量方向等于结束状态方向；没有回摆、过冲和二次修正。
- 节点、文字、子树包围盒不发生未声明的重叠。
- 边始终连接正确的对象和可见边界，不穿过节点或悬空。
- 所有可见对象在视口、字幕区和相机裁切范围内。
- 相邻帧中每个对象都有连续位置、尺寸和透明度；任何大幅变化都能指向一个有意设计的镜头边界或阶段动作。
- 轨迹速度符合镜头卡中的运动预算；快速对象也必须沿连续路径运动，而不是从起点跳到终点。

### 8.3 真实像素和视频检查

检查源 SVG 或 Python 输出不够，必须检查最终实际媒体：

1. 从 WebM/MP4 抽取开头、结尾、每个阶段边界前后以及随机中间帧。
2. 在边界附近连续抽取一段帧带，逐帧观察是否存在“大幅换图”。
3. 用 Pillow 或图像检查工具检查画布边缘、裁切、节点重叠、文字遮挡和边断裂。
4. 以正常速度播放一遍，再以慢速播放一遍。正常速度检查叙事和节奏，慢速检查轨迹、交接和接缝。
5. 用 `ffprobe` 检查帧率、时长、分辨率、像素格式、alpha、音视频流和编码参数。
6. 检查视频实际修改时间晚于依赖它的最新脚本、录音和视觉资产；共享函数变更后不能沿用旧 WebM。
7. 将检查帧、状态差异、媒体参数和构建命令作为本次产出的证据保存或至少记录在构建日志中。

#### 强制帧带读取法

对于本项目的 24 fps 视频，图片检查统一使用以下采样口径：

- 沿视频帧序列以 **8 帧为步长**抽取一帧，即使用 `0, 8, 16, ...` 这样的帧索引；“间隔 8 帧”在这里明确表示采样步长为 8，不是额外跳过 8 帧后再取第 9 帧。
- 每次将连续的 **9 张采样帧**拼接成一张帧带图。若本组首帧索引是 `k`，则本组包含 `k, k+8, k+16, ..., k+64`，覆盖 65 个原始帧，约 2.67 秒。
- 拼接完成后，先降低拼接图的输出质量，再进行读取检查。推荐将帧带缩小到适合一次查看的尺寸，并以 JPEG 质量 45--60 导出；压缩只用于提高快速浏览的可读性和暴露整体跳变，不得替代原始帧检查。
- 每组帧带必须保留帧索引或时间戳标注，至少标出首帧和末帧，确保发现问题后能够回到原始 WebM/MP4 定位。
- 默认按非重叠的 9 张采样帧分组：第一组从 `0` 开始，下一组从上一组最后一张采样帧之后的下一采样点开始，即 `0--64`、`72--136`、`144--208`。视频开头、结尾、镜头边界和阶段边界必须额外生成帧带，不能因为不落在自然分组中而跳过。
- 视频结尾不足 9 张采样帧时保留一个不完整的尾组，并在清单中记录实际帧数；尾组用于确认最后状态和是否提前跳变，不能用重复帧伪装成完整 9 帧。
- 先读取降质后的 9 帧拼接图，检查是否存在整体跳动、物体瞬移、过冲回弹、画面替换、边断裂和构图越界；发现异常后，再读取对应的原始帧和相邻原始帧确认根因。
- 不能只拼接 9 张静态关键帧，也不能把抽取出来的 9 张图当成视频的全部检查。该方法是快速发现问题的固定筛查层，逐帧检查和阶段边界检查仍然有效。

项目提供 `scripts/check_video_frames.py` 自动执行上述流程。24 fps 成片使用：

```sh
python3 scripts/check_video_frames.py \
  outputs/avl-video/avl-recap.mp4 \
  outputs/avl-video/frame-check
```

脚本默认使用步长 `8`、每组 `9` 帧、3×3 排列和 JPEG 质量 `50`，并生成带帧号/时间戳的 JPEG 帧带及 `manifest.json`。检查其他帧率的独立素材时，必须把实际帧率传给 `--fps`，例如 `--fps 30`。

### 8.4 跳帧的专门诊断

发现画面跳动时，按以下顺序定位，不要先增加 easing 或延长时长：

1. 比较跳动帧前后的语义状态，确认是否发生了未声明的拓扑或对象 ID 变化。
2. 比较几何状态，找出哪个对象的坐标、角度、尺寸或透明度出现非连续变化。
3. 查找该属性的所有写入者，确认是否存在旋转、布局、相机或渲染层的重复调整。
4. 检查阶段边界是否同时被两个分支绘制，或有一个分支漏掉了中间状态。
5. 检查是否在合成阶段跳取了源视频帧，或者因为整数取整、帧率换算造成了重复/跳过。
6. 只有确认状态和轨迹正确后，才调整节奏、easing 或停顿时长。

## 九、合格标准

一个动画只有同时满足以下条件，才算符合本方法论：

### 必须满足

- 镜头边界明确；镜头内没有未经设计的整图切换。
- 开始状态和结束状态都是可验证的规范状态。
- 镜头内从第一帧到最后一帧存在连续的逐帧状态。
- 每个可见对象有稳定身份；新增、删除、交接都有可解释的视觉过程。
- 旋转角度、旋转中心、缩放、平移和结构距离由前后状态及真实布局推导，不依赖固定操作角度或固定结构偏移。
- 不存在“先过冲，再由其他代码调回目标”的补偿运动。
- 每个运动属性只有一个明确的所有者，渲染层没有隐藏修正。
- 阶段交界处状态相接；没有因为 `if/else` 边界或时间取整丢失中间状态。
- 节点、文字、边和子树不重叠、不越界、不悬空，且教学语义没有被运动误导。
- 真实渲染媒体已经逐帧或按阶段边界检查，而不是只检查源代码、SVG 或少数关键帧。
- 资产可以从项目内脚本可重复生成；共享依赖修改后所有下游资产都已更新。
- 当前配音、ASR 时间线、镜头边界和视觉资产彼此对应，最终成片使用的是最新版本。

### 一票否决

出现以下任何一项，动画不进入成片：

- 旋转过头后又回到目标位置。
- 镜头中间突然换成完整的后状态。
- 节点或子树从一个位置瞬移到另一个位置。
- 代码无法说明某个对象在跳动帧之间发生了什么。
- 为了隐藏不连续而使用没有语义依据的交叉淡化。
- 画面虽然“看起来差不多”，但结束状态不是算法要求的状态。
- 只渲染或检查了静态图，没有检查实际 WebM/MP4。
- 修改了共享动画函数，却没有重生成全部依赖资产。

### 可以接受的情况

- 有意的硬切，但它发生在明确的镜头边界，并且新镜头重新建立了自己的开始状态。
- 有意的保持帧，前提是保持本身服务于口播阅读，不是用来遮盖缺失的中间运动。
- 数值舍入带来的不可见误差，前提是误差不改变语义、不造成可见跳动，且末帧回到规范状态。
- 为了构图使用固定的画布尺寸、节点视觉尺寸、字体和帧率；这些是表现参数，不得冒充结构布局参数。

## 十、交付前最小记录

每个独立动画至少留下以下记录：


```text
资产名称：
生成命令：
源脚本：
依赖的共享函数：
镜头/阶段列表：
开始状态与结束状态：
关键不变量：
角度和布局的推导方式：
逐帧检查结果：
阶段边界检查帧：
ffprobe 参数：
已重生成的下游资产：
未解决风险：
```

最终原则只有一句话：**先定义可验证的前后状态，再由状态求解布局和运动；能连续解释的放进同一个镜头，不能连续解释的明确切镜头；每一帧都必须是计划的采样结果，而不是上一张图和下一张图之间的补丁。**
